# =============================================================================
# vodafonepaycomtr (Next.js site) — CI/CD → TEST OpenShift (tst-vcloud, namespace vepas-ai-am)
#
# devops-proje-sablonu/.github/workflows/pipeline-test.yml şablonundan npm/Next.js
# projesine uyarlandı — bkz. clover reposundaki docs/OCP-DEVOPS-RUNBOOK.md §2
# (aynı kararlar bu servis için de geçerli).
#
# ⚠️ PUSH NOTU: GitHub, .github/workflows/ altındaki dosyaların HTTPS + OAuth/PAT ile
#    push edilmesini token'da `workflow` scope'u yoksa REDDEDER. Çözüm (biri yeterli):
#      1) SSH remote:  git remote set-url origin git@github.vpara.local:<org>/vodafonepaycomtr.git
#      2) Classic PAT: `repo` + `workflow` scope'ları
#      3) Fine-grained PAT: Repository permissions > Workflows: Read and write
#      4) Dosyayı GHES web arayüzünden oluştur
#
# ⚠️ HENÜZ TEYİT EDİLMEDİ:
#   - GHES repo kaydı (bu pipeline'ın çalışması için önce bu gerekiyor)
#   - `runs-on: MENDPROJECT-Vepas` (mend.yml) — gerçek Mend runner etiketi DevOps'tan
#     teyit edilmeli
#   - Image pull secret / registry kullanıcı adı-token
# =============================================================================
name: CI/CD (test)

on:
  push:
    branches: [development, release]
  pull_request:
    branches: [development, release, master, main]
  workflow_dispatch: {}

env:
  APP_NAME: vodafonepaycomtr

  NODE_EXTRA_CA_CERTS: /etc/pki/tls/certs/ca-bundle.crt

  OPENSHIFT_SERVER:        ${{ vars.OPENSHIFT_SERVER_TST }}
  OPENSHIFT_TEST_USER:     ${{ vars.OPENSHIFT_TEST_USER }}
  OPENSHIFT_TEST_PASSWORD: ${{ secrets.OPENSHIFT_TEST_PASSWORD }}
  OPENSHIFT_NAMESPACE:     ${{ vars.OPENSHIFT_PROJECT_AD }}        # vepas-ai-am

  REGISTRY_URL: containers.github.vpara.local

jobs:
  # ---------------------------------------------------------------------------
  # 1) BUILD + TEST
  # ---------------------------------------------------------------------------
  build-test:
    name: Build & Test
    runs-on: githubrunner
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run typecheck

      - name: Test
        run: npm run test

      # 31.08.2026 Clover'da sahada bulundu, KÖK SEBEP node_modules/next/dist/build/
      # swc/index.js + .../lib/download-swc.js OKUNARAK doğrulandı (aynı runner, aynı
      # sorun burada da beklenir):
      #
      #   1) Runner'ın glibc'i (RHEL) native SWC binary'sinin (@next/swc-linux-x64-gnu)
      #      istediği GLIBC_2.29'dan eski; musl alternatifi de host'a uymuyor.
      #   2) Native başarısız olunca Next, wasm fallback'ı ÖNCE "yerelde var mı" diye
      #      dener — ama bu kontrol `pathToFileURL('@next/swc-wasm-nodejs')` kullanıyor,
      #      node_modules bare-specifier çözümü DEĞİL, cwd'ye göre düz bir dosya yolu
      #      (doğrulandı) — yani paketi `npm install` ile node_modules'a koymak
      #      (Clover'da ilk denenen, İŞE YARAMAYAN çözüm) bu kontrol tarafından HİÇ
      #      görülmüyor.
      #   3) Next doğrudan `downloadWasmSwc()`'e düşüyor — o da npm'in registry/proxy
      #      ayarlarını KULLANMAYAN çıplak bir `fetch()` ile tarball indirmeye
      #      çalışıyor; kapalı ağda bu timeout'la patlıyor.
      #
      # Çözüm: `downloadWasmSwc()`'in indirmeden önce `fs.existsSync()` ile kontrol
      # ettiği tam hedef klasörü (`node_modules/next/wasm/@next/swc-wasm-nodejs/`)
      # BİZ, npm'in zaten çalışan `npm pack` komutuyla önceden dolduruyoruz.
      - name: Pre-populate SWC wasm fallback (offline-safe, avoids next build's own fetch())
        shell: bash
        run: |
          set -e
          NEXT_VERSION="$(node -p "require('./node_modules/next/package.json').version")"
          echo "[INFO] Next.js resolved version: ${NEXT_VERSION}"
          PKG_NAME="@next/swc-wasm-nodejs"
          TARBALL_PATH="$(npm pack "${PKG_NAME}@${NEXT_VERSION}" --json | node -p 'JSON.parse(require("fs").readFileSync(0,"utf8"))[0].filename')"
          WASM_DIR="node_modules/next/wasm/${PKG_NAME}"
          mkdir -p "${WASM_DIR}"
          tar -xzf "${TARBALL_PATH}" -C "${WASM_DIR}" --strip-components=1
          rm -f "${TARBALL_PATH}"
          echo "[INFO] ${WASM_DIR} dolduruldu — next build artık ağa çıkmadan bunu kullanacak."

      # --webpack ZORUNLU burada: Clover'da sahada bulundu (aynı runner, aynı sorun
      # beklenir) — SWC wasm fallback next.config.ts'i transpile etmeyi başarıyor,
      # ama Turbopack (varsayılan builder) SADECE native binding'lerle çalışıyor,
      # WASM binding yüklendiğinde Next'in kendi hatası `--webpack` kullanmayı
      # öneriyor. `npm run build` yerel geliştirmede Turbopack kullanmaya devam
      # etsin diye package.json'a dokunmadık — sadece bu CI adımı Webpack'e düşüyor.
      - name: Build
        run: npx next build --webpack

  # ---------------------------------------------------------------------------
  # 2) GÜVENLİK TARAMALARI — build-test ile PARALEL
  # ---------------------------------------------------------------------------
  fortify-scan:
    name: Fortify SAST
    uses: ./.github/workflows/fortify.yml
    with:
      env_name: vepas-ai-am
      source_repo: ${{ github.repository }}
      project_type: npm
      fortify_blocker: "0"
    secrets: inherit

  mend-scan:
    name: Mend SCA
    uses: ./.github/workflows/mend.yml
    with:
      target_branch: ${{ github.ref_name }}
      env_name: vepas-ai-am
      project_type: npm
    secrets: inherit

  sonar-scan:
    name: SonarQube
    uses: ./.github/workflows/sonar.yml
    with:
      env_name: vepas-ai-am
      project_type: npm
      sonar_blocker: "false"
    secrets: inherit

  # ---------------------------------------------------------------------------
  # 3) IMAGE BUILD + TEST OCP DEPLOY
  # ---------------------------------------------------------------------------
  build-and-deploy:
    name: Build Image & Deploy to TEST
    runs-on: githubrunner
    needs: build-test
    if: >-
      github.event_name != 'pull_request' && (
        github.ref == 'refs/heads/development' ||
        github.ref == 'refs/heads/release' ||
        github.ref == 'refs/heads/main'
      )
    steps:
      - uses: actions/checkout@v4

      - id: img
        run: echo "image=${{ env.REGISTRY_URL }}/${{ github.repository_owner }}/${{ env.APP_NAME }}:${{ github.sha }}" >> "$GITHUB_OUTPUT"

      - name: Detect container engine
        id: engine
        shell: bash
        run: |
          set -e
          if command -v podman >/dev/null 2>&1; then echo "bin=podman" >> "$GITHUB_OUTPUT"
          elif command -v docker >/dev/null 2>&1; then echo "bin=docker" >> "$GITHUB_OUTPUT"
          else echo "::error::Runner'da ne podman ne docker var — imaj build edilemez."; exit 1; fi

      - name: Log in to Container Registry
        run: |
          echo "${{ secrets.REGISTRY_LOGIN_TOKEN }}" | ${{ steps.engine.outputs.bin }} login "${{ env.REGISTRY_URL }}" \
            -u "${{ vars.REGISTRY_LOGIN_USERNAME }}" --password-stdin

      # 01.09.2026 Clover'da sahada bulundu (aynı runner, aynı sorun burada da
      # beklenir): `docker build`'in içindeki `npm ci` runner'ın kendi
      # adımının aksine host'un proxy config'ini miras almıyor — izole build
      # container'ının bu kapalı ağda registry.npmjs.org'a doğrudan çıkışı yok,
      # ağa çıkmaya çalışıp süresiz asılı kalıyordu. Çözüm: org'un zaten
      # tanımlı `NPM_PROXY_SETTING` değişkenini gerçek değerlerle doldurup
      # `.npmrc` olarak build context'e yaz — Containerfile bunu `npm ci`
      # öncesi zaten COPY'liyordu (`.npmrc*`, Vercel'in kendi şablonundan),
      # şimdi install sonrası SİLİYOR da (image'a parola gömülmesin).
      - name: Create .npmrc from NPM_PROXY_SETTING (npm ci'nin Docker build içindeki proxy erişimi için)
        shell: bash
        env:
          MEND_PROXY_USER: ${{ vars.MEND_PROXY_USER }}
          MEND_PROXY_PASSWORD: ${{ secrets.MEND_PROXY_PASSWORD }}
        run: |
          set -e
          RAW_SETTING='${{ vars.NPM_PROXY_SETTING }}'
          if [[ -z "$RAW_SETTING" ]]; then
            echo "::error::vars.NPM_PROXY_SETTING tanımlı değil — npm ci Docker build içinde ağa çıkamayacak."
            exit 1
          fi
          # user:pass bir URL'nin içine gömülüyor (proxy=http://user:pass@host).
          # Parola @, :, / gibi URL-özel karakterler içeriyorsa çıplak substitution
          # URL'yi bozar (Clover'da 01.09.2026 sahada bulundu). Percent-encode
          # ediyoruz — parolanın gerçek değeri her ne olursa olsun sonuç her
          # zaman geçerli bir URL parçası.
          ENCODED_USER="$(node -e 'process.stdout.write(encodeURIComponent(process.env.MEND_PROXY_USER))')"
          ENCODED_PASSWORD="$(node -e 'process.stdout.write(encodeURIComponent(process.env.MEND_PROXY_PASSWORD))')"
          echo "$RAW_SETTING" > .npmrc
          sed -i "s/{{MEND_PROXY_USER}}/${ENCODED_USER}/g" .npmrc
          sed -i "s|{{MEND_PROXY_PASSWORD}}|${ENCODED_PASSWORD}|g" .npmrc

      - name: Build image
        run: ${{ steps.engine.outputs.bin }} build -t "${{ steps.img.outputs.image }}" -f Containerfile .

      - name: Remove .npmrc from runner workspace
        if: always()
        run: rm -f .npmrc

      - name: Push image
        run: ${{ steps.engine.outputs.bin }} push "${{ steps.img.outputs.image }}"

      - name: Ensure OpenShift CLI (oc)
        shell: bash
        run: |
          set -e
          command -v oc >/dev/null 2>&1 && { oc version --client | head -1; exit 0; }
          curl -fsSL https://mirror.openshift.com/pub/openshift-v4/clients/ocp/stable/openshift-client-linux.tar.gz -o "${RUNNER_TEMP}/oc.tar.gz"
          tar -xzf "${RUNNER_TEMP}/oc.tar.gz" -C "${RUNNER_TEMP}" oc
          echo "${RUNNER_TEMP}" >> "${GITHUB_PATH}"

      - name: Log in to OpenShift
        run: |
          oc login "${OPENSHIFT_SERVER}" --insecure-skip-tls-verify=true \
            -u "${OPENSHIFT_TEST_USER}" -p "${OPENSHIFT_TEST_PASSWORD}"
          oc project "${OPENSHIFT_NAMESPACE}"

      # ⚠️ ConfigMap (vodafonepaycomtr-config), Secret (vodafonepaycomtr-secret) ve
      #    Route BURADA UYGULANMAZ — Faz 0'da elle, BİR KEZ kurulur (bkz.
      #    DEPLOYMENT_RUNBOOK.md), pipeline onlara bir daha dokunmaz.
      #
      # ⚠️ CA sertifika ConfigMap adımı YOK: S5 kararı — TLS/CA gerekmiyor.

      - name: Apply ServiceAccount
        run: oc apply -f k8s/serviceaccount.yaml

      - name: Apply Service
        run: oc apply -f k8s/service.yaml

      - name: Apply HPA
        run: oc apply -f k8s/hpa.yaml || true

      # NetworkPolicy KASTEN yorumda: k8s/networkpolicy.yaml şu haliyle placeholder.
      # - name: Apply NetworkPolicy
      #   run: oc apply -f k8s/networkpolicy.yaml

      - name: Apply Deployment
        env:
          IMAGE_REF: ${{ steps.img.outputs.image }}
        run: envsubst < k8s/deployment.yaml | oc apply -f -

      - name: Rollout status
        run: oc rollout status deployment/${APP_NAME} --timeout=300s

      - name: Verify deployment
        run: |
          oc get pods -l app=${APP_NAME} -o wide
          ROUTE_HOST=$(oc get route ${APP_NAME} -o jsonpath='{.spec.host}' 2>/dev/null || echo "not found")
          echo "Liveness:  https://${ROUTE_HOST}/api/health/liveness"
          echo "Readiness: https://${ROUTE_HOST}/api/health/readiness"
