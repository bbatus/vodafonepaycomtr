# host KASTEN YOK — otomatik üretilecek: (bkz. clover/k8s/route.yaml'daki
# aynı açıklama, kullanıcının verdiği finwatcher-frontend örneğiyle aynı kalıp)
#   vodafonepaycomtr-vepas-ai-am.apps.tst-vcloud.vpara.local
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: vodafonepaycomtr
  namespace: vepas-ai-am
  labels: { app: vodafonepaycomtr }
spec:
  # host: vodafonepaycomtr-vepas-ai-am.apps.tst-vcloud.vpara.local
  to: { kind: Service, name: vodafonepaycomtr, weight: 100 }
  port: { targetPort: http }
  tls: { termination: edge, insecureEdgeTerminationPolicy: Redirect }
