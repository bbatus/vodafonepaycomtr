# imagePullSecrets — ZORUNLU, containers.github.vpara.local private registry.
# `vodafone-githubtest`, namespace'te ZATEN VAR OLAN, aynı registry için
# çalışan bir secret — genaiops-event-processor'ın kendi ServiceAccount'unda
# (01.09.2026 Clover'da sahada doğrulandı) BİREBİR AYNI şekilde referans
# veriliyor ve sorunsuz çalışıyor. Bu servise özel yeni bir pull secret
# oluşturulmasına gerek yok.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vodafonepaycomtr
  namespace: vepas-ai-am
  labels: { app: vodafonepaycomtr }
imagePullSecrets:
  - name: vodafone-githubtest
